{
  "spec": {
    "template": {
      "spec": {
        "containers": [{
          "name": "backend",
          "env": [{
            "name": "TIMEOUT",
            "value": "600"
          }]
        }]
      }
    }
  }
}
